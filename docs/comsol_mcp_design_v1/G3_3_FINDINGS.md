> Delivery update: repaired source snapshot `84ec0962a6424cd54c523770e4898bf42b7af645` has passed fresh extraction, wheel rebuild, and physically outside-source installation (64 source/package files, 272 operations, four Java resources; pip check clean). Remote synchronization and final documentary archive remain pending. See `evidence/phase4_3/runs/final_recovery_84ec096_20260922T095300Z/final_recovery_receipt.json`.

# G3.3 findings and corrections

Current status: **RUNTIME_VALIDATED_DELIVERY_PENDING** at source `a3f39b3022b417e16c3087b8b35f1daa06f30b8a`. The `032c0a5` native table failure is repaired in `ec02c12` / `4c32d75` and the verified native accessor correction is at `a3f39b3`. All eight final6 public-MCP groups passed with unchanged source manifests; full pytest passed 1824 tests with one Windows-only skip; the source-outside wheel/resource check passed; and task-owned cleanup finished with a PASS inventory containing zero selected processes. Repaired-source recovery/archive, clean publication, and normal remote synchronization remain pending, so overall acceptance remains false. Prior findings and claims are preserved in Git and the resumption review directory; this document reports the current implementation.

| Finding | Repair and verification boundary | Cases |
|---|---|---|
| F01 Evidence | Historical mocks remain CONTROL_UNIT/UNIT_CONTRACT; 600 protected files byte-match PIN. Fresh live runs and source hashes are separate. | C00, C01 |
| F02 Measure/statistics | Shared MeasureSpec uses actual dimensional measure and selections, integral denominators, centered variance, RMS and positive weights. Failed selection writes are attempted once and retain uncertainty; exception text is not an entity readback. | C04–C06 |
| F03 Axisymmetry | Explicit native revolved/cross-section settings and independent cylinder oracles prove the physical measure without an extra radial factor. | C07 |
| F04 Solution axes | SolutionBinding and FieldArray carry expression/outer/inner/point axes and native per-outer metadata; selectors preserve axis identity. | C08 |
| F05 Complex values | Confirm real/complex status, refuse missing or mismatched imaginary values, and define transform-before-statistics behavior. Failure injection remains CONTROL evidence. | C09 |
| F06 Coordinates | Shared validation converts physical units, checks dimensions/frame and compares actual readback positions and shape. Swapped/nonfinite readback injection is CONTROL evidence. | C10 |
| F07 Typed dataset chains | Dataset binding follows upstream references and detects cycles/ambiguity; typed node paths and sequential write outcomes protect other collections. | C11 |
| F08 Export authorization | ArtifactStore authorizes project containment, symlinks and overwrite before computation/publication. | C12 |
| F09 Failure/atomicity | Unknown, failed or cleanup-failed evaluation cannot publish; replacement is atomic and non-overwrite publication cannot clobber a racing target. Native evaluation failure is live; cleanup fault injection is CONTROL only. | C02, C12 |
| F10 Definitions Probe | Native probes are separate from Derived Values; GlobalVariable receives its actual component owner. Real transient history returns native labeled time values, provenance and aligned pagination, not inferred stored-output times. | C14 |
| F11 Bounded artifacts | Public chunk access hashes and seeks through the same descriptor with identity checks; engine allocation budget rejects oversized payloads before getData. Driver RSS is sampled; engine cache is UNMEASURED. | C13 |
| F12 Stored-solution reopen | Three fresh artifacts reopen without solving in new Workers. Seven negative controls include real clearSolutionData with retained dataset, changed field and missing Derived Values. Empty dataset inventory alone means DATASET_NOT_FOUND. | C03 |

## Resumption defects fixed at 1b5e80e

The c051065 19-case certificate did not establish complete NEXT_GOAL acceptance: its Probe history was empty, its identical-retry flag was false, and semantic review/publication were incomplete. The new public MCP driver verifies those contracts independently. Selection exceptions no longer fabricate empty entity lists or trigger a second native setter. The cleared-solution checker no longer infers a clear operation from an empty Results tree.

The native Probe table exposes a `时间 (s)` column and 23 actual solver-step rows for the fixture, while its stored output solution has five times. Production history now carries the table axis explicitly and does not substitute the five output times. Missing, ambiguous, nonfinite, complex or malformed time columns remain UNVERIFIED.

The actual native failing-study test preserves `execution_state_unknown`, the cause and reconciliation demand; exact-key retry retains the original job/operation and unchanged complete event log. A readable timestamp does not erase engine uncertainty.

Final exact run paths, software counts, source binding and remaining scope limits belong in `evidence/phase4_3_acceptance.json`. No six-platform or unrestricted-module certification is claimed.

## Failure-path corrections after the first live pass

At `ed5a566` and `032c0a5`, table mutations gained actual exact real/imaginary/header comparisons. Readable data alone no longer means a matching write; a real-only rewrite requires native complex-status proof and cannot silently retain imaginary data. Required dataset edges cannot disappear when native getters fail, and incomplete legacy solution axes no longer claim a complete binding. The legacy Interp path uses one verified coordinate setter and stops after a failed run. Axisymmetric temporary measure restoration failures are surfaced.

The domain classifier now requires both explicit validation-stage provenance and a no-mutation witness for a clean refusal. Contradictory or missing proof remains UNKNOWN without inventing a dispatch stage. Java serialization rejects nonfinite results as a structured failure while preserving request identity and post-dispatch uncertainty; actual JVM codec controls cover finite, NaN, infinity and nested values.

## Native regression after exact table comparisons

The fresh `final3_nodes_032c0a5_20260922T0813Z` run found that final create/set readback omitted the requested target. Per-step setters matched, but the completion layer coerced an unspecified final match to false and the public operation failed. Later revision conflicts were a cascade from that first failure. The software suite at the same source passed 1816 tests with one Windows-only skip; this did not certify native correctness. The failure remains preserved, and the repair must verify all requested values together at final readback and exercise public outcome classification.

The correction verifies the final table state against every requested value and native identity, after all setters have finished. The new regression passes actual table create/set results through the production domain classifier. Intermediate native nodes and numeric groups passed at `4c32d75`; the final a3f39b3 batch subsequently passed all eight groups. Read-only dataset inspection reports graph-derived solution identity and explicit partial metadata; an unsolved dataset is still inspectable and does not acquire a false complete numerical binding.

## Final runtime and software candidate

At `a3f39b3`, all eight `final6_*_a3f39b3_20260922T0907Z` native groups passed without source drift. Full pytest passed 1824 tests with one Windows-only skip; the wheel installed outside the source tree and passed resource/hash checks. A full-suite failure at `4c32d75` exposed a speculative `getTag` fallback; it was removed, leaving the verified native `tag()` API. Cleanup is now recorded by `evidence/phase4_3/runs/final_batch_a3f39b3_20260922T0907Z/final_process_inventory_after_cleanup.json` with zero selected processes remaining. These receipts establish the stated runtime/software/cleanup scopes but do not yet prove repaired-source recovery, a clean publication snapshot, or remote synchronization.

Publication remains a separate delivery step: preserve the original development history locally, create a new snapshot as a child of verified `origin/main` PIN `2cb4627924d1a3240818ea7cd00453d4bd2d2da8`, recheck the same tested source hashes, exclude the three historical worker bearer-token blobs, and push without force. No future snapshot SHA or archive is claimed.
