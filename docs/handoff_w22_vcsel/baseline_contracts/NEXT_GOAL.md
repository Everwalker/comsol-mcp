# 当前Goal：W20冻结收口，随后推进W21

## 1. 本轮目的与不变项
停止“每轮全仓重新审计→加要求→仍不能毕业”的循环。此次仅更改工作管理规则，保留原16项/27目标、Windows6.3和6.4、原基准与容差、主Agent开发＋独立Reviewer。

依次阅读 START_HERE.md、BLOCKER_POLICY.md、TEAM_PROTOCOL.md、contract/W20_ACCEPTANCE.md、contract/W20_BENCHMARKS.md、W21_PLAN.md。原始规则文件按 CONTRACT_LOCK.json 校验；这是版本固定，不是新证明系统。

真实原生缺口不能延期为已完成：例如伪造观测可验证、数值PASS被升级为physical PASS、固定列表冒充真实精化、越权写报告、重开后用旧缓存值，这些若仍存在，应修复对应原条目。不要把它们重新描述为“只是范围扩大”。

## 2. 恢复与继承进度
在新工作包目录运行 verify_package，再 bootstrap。固定公开源沿用上一包a85eccd0…，不是本次重新认证的最新代码。恢复到新repository/，本轮指令在 docs/handoff_w20_closure_then_w21/。

若已有更新且真实可访问的源码/进度，先保留、核对与PIN差异再继续，不重置、不覆盖。无需旧目录才能启动；没有更新来源时使用PIN。不会恢复未提交/ignored科学数据或凭据。

重新发现Windows两个安装、Python/JDK、许可证和任务私有运行目录；不复用旧PID、端口、token、回执或venv。保持公共Mac架构，Mac/GUI/其他架构没有新资源时不纳入W20额外前置。

读取恢复后的原始AGENTS与设计计划，历史停止边界由本文件覆盖；不要更改安全规则或用户全局Agent配置。先运行适当的软件基线和最小生产链，避免错误环境导致整套反复空跑。

## 3. 只做一次 W20差距表，不重开全仓审计
对原T01–T16的27目标逐项记录：已有实现、原始证据与源码适用性、PASS/FAIL/NOT_RUN/BLOCKED、最小待办。使用PROGRESS.md或现有主台账中的一个表，不创建第二套相同记录。

原APPROVED/计数不是自动通过。可靠旧证据可以复用：记录实际来源及当前改动是否影响它；规则/相关实现未改、不依赖失效资源的结果无需重跑。被当前改动影响的那部分重测，不因无关文档更改重置全部27项。T03/T06/T09/T11/T13/T14等原要求的独立关键复测保持，只做与冻结判据相关的正负控。

现有Review/FINDINGS用于查对应代码，不作为无限功能愿望清单。不要在功能未变的历史目录、别的平台或不相关模块主动寻找新毕业条件。

## 4. 主Agent实现与Reviewer闭环
主Agent自己设计、编码、debug、运行测试。按依赖选择最短闭环，例如真实采样→后端登记→验证→报告，再处理真实精化和新Worker重开。复用W17/18/19，不另建平行后端或数据库。

建立一个由Host实际创建的独立Reviewer上下文，提供冻结标准、源码/候选与证据路径。会话是真实独立的；不得自己写一个随机session_id声称已经独立。默认Reviewer不改生产代码，可编写反例。

每轮候选清单至少可识别生产代码、runner、schema、oracle与实际依赖。可复用原Git提交＋必要dirty manifest；不用再建设自定义签字/证明平台。主Agent提交简明差距和实际日志，Reviewer必须自行看代码、证据并按原判据复测，不能只阅读主Agent总结。

Reviewer返回：W20_APPROVED、CHANGES_REQUIRED（列冻结条目或合格关键风险）、BLOCKED_ENVIRONMENT、REVIEW_BLOCKED。不是“继续优化”或“还可以更好”。严格按 BLOCKER_POLICY 执行：新非阻断发现入待办，不能拖住当前阶段。

相同缺陷两轮未解决时，将其缩成最小纵向复现并换方法；不重复跑整套验收、不放宽断言、也不另增关卡。不要设置“达到N轮即通过”的自动规则。

## 5. 明确毕业规则
**同时满足以下条件就关闭W20：**
1. 原16项及27目标的必需标准有对应等级的有效证据；
2. 独立Reviewer完成原规定的逐项复核和关键复测；
3. 当前候选没有满足 BLOCKER_POLICY 的未解决阻断项。

满足后Reviewer必须签署当前候选W20_APPROVED，即使DEFERRED_BACKLOG不为空；不能以新的风格/架构/效率/全平台愿望拒签。主Agent不能代签或用文件校验器代替该判断。

BLOCKED、只有mock、证据缺失不满足毕业条件。真实运行环境缺失只阻塞依赖它的步骤；完成其他可执行的W20工作后交付状态与最小解锁动作，不声称毕业、也不进入依赖它的W21执行。

## 6. 批准后立即进入W21
批准前不实现W21来掩盖W20缺口；批准后**无需再问用户是否继续**，按 W21_PLAN.md 完成原计划的扫描、有限优化、case/cache/预算管理和通用阶段状态传递。仅发生需要额外系统/网络/许可权限的操作时遵守正常授权边界。

W21有自己的固定范围，W20未改动部分不被整体重新开审；若W21变更触发W20回归，只复测受影响条目，修复后继续。不抹掉之前W20批准历史，不给项目又命名一串G3.x阻塞轮次。

W21通用能力不等于完整胶水物理案例、所有模块、所有OS认证。按W21_PLAN的来源映射清楚报告T021完成、T047通用传递子项与仍待W24的领域部分；不能把子项通过登记成原T047全量通过。

## 7. 交付、停止与保全
持续更新单一PROGRESS，待办进入DEFERRED_BACKLOG；必要原始日志/模型/哈希沿用仓库规范。报告器只汇总实测，不填默认PASS/固定observed。安全/文件/进程操作只在已授权且归属本任务的对象进行，不能改全机策略或停止共享Server。

保留源码、测试、参数化配置和可重建模型/数据，秘密文件不公开。按已有授权普通非force同步；无权限交付本地commit/patch并准确说明。恢复后的产物可以从固定公开源＋差异或最终公开SHA重建，不能只交更新后的报告。

**最终停止点：W21本包范围实现并验收完毕，Reviewer批准后交付；不自动进入W22。**成功报告包括W20关闭依据、W21具体成果、未实现领域/平台边界和非阻断待办。

若仍有真实阻断，交付已完成代码和证据，明确哪些冻结条目未通过。不能因为目标很大只停在规划，也不能为了“推进”牺牲真实性。
