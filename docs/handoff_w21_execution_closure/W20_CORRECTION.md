# Limited correction to inherited W20 acceptance

This appendix preserves the historical signoff in `docs/handoff_w20_closure_then_w21/INDEPENDENT_REVIEW_VERDICT.md`. It corrects only the explicitly identified W21 dependencies; it does not reopen the 27-target historical acceptance matrix. The frozen contract remains byte-identical.

- **T06, T09, T12:** prior source accepted caller observation mappings/values and could promote numerical results to physical PASS. Current production sampling registers backend-owned W17 observations with successful operation provenance, exact runtime/model/revision binding, stored-solution computation identity and artifact hash. Caller replacement/forged references and stale live observations are refused. Analytical benchmark success remains numerical only; physical validation is UNVERIFIED. Independent public negative and normal tests are documented in REVIEWER_NATIVE_ROUND1/2 and their raw Windows transcripts.
- **T11:** the historical fixed `[.065,.028,.009]` list was not native convergence evidence. Real three-grid/three-tolerance B2 computations now exist for both builds, with element/DOF/settings readback and nine-point stored fields. The new convergence references resolve backend-owned error/settings records and reject unregistered lists, duplicate references, duplicate native settings and error overrides. Final candidate main runs: `closure_t11_63_01` and `closure_t11_64_03`. Original failed runs `closure_t11_64_01/02` remain preserved: the first exposed a runner stale-ref error; the second exposed a domain-status error that froze later requests. The final public negative calls are explicit FAIL with clean follow-up execution. Independent final native review remains the authority for closure.
- **T14:** independent reviewer same-version fresh-server/Worker reopen runs loaded exact-SHA saved models without solving, generated fresh W17 references and compared stored values exactly. Missing-file and missing-solution negatives were rejected. See `reviewer63_reopen01` and `reviewer64_reopen01`; unrelated T14 behavior is unchanged by the T11-only final source delta.

Other effective W20 requirements inherit their applicable prior evidence. No fabricated temperature, version string or hash-only assertion is counted as native execution. W21 validates only the generic T047 transfer subitem; full gel/UV/stress acceptance remains NOT_RUN and belongs to W24.
